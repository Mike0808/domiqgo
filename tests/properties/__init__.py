"""Тесты модуля Properties — шаг C3 плана миграции.

- `test_property.py` — домен. Ни базы, ни Django, ни фикстуры `db`.
- `test_api.py` — публичная поверхность модуля целиком, через базу, включая
  события.

Инвариант норматива подогрева здесь пока не проверяется: он действует с
исправления дефекта №29, но живёт в `Apartment.clean` устаревающего слоя и
проверяется характеризующим тестом. В домен модуля он переезжает шагом C3c —
тогда же сюда приедет и его проверка.
"""
