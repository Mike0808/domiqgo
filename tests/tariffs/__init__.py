"""Тесты модуля Tariffs — шаг C1 плана миграции.

Разделены по слоям, и это не формальность:

- `test_schedule.py` — домен. Ни базы, ни Django, ни фикстуры `db`. Если
  когда-нибудь этому файлу понадобится `pytest.mark.django_db`, значит правило
  утекло из `domain/` в хранилище.
- `test_api.py` — публичная поверхность модуля целиком, через базу.
"""
