"""Тесты модуля Metering — шаг C2 плана миграции.

`test_api.py` — публичная поверхность модуля целиком, через базу.

Отдельного файла на домен пока нет, и это не упущение: на шаге C2c в
`domain/` лежит только каталог видов ресурса. Правила учёта — база отсчёта и
монотонность показаний — переезжают из `billing/services/` шагом **C2d**;
вместе с ними появится и `test_point.py` без базы, как `test_schedule.py` у
Tariffs.
"""
