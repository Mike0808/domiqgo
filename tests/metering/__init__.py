"""Тесты модуля Metering — шаг C2 плана миграции.

- `test_point.py` — домен. Ни базы, ни Django, ни фикстуры `db`. Если этому
  файлу когда-нибудь понадобится `pytest.mark.django_db`, значит правило
  утекло из `domain/` в хранилище.
- `test_api.py` — публичная поверхность модуля целиком, через базу.

До шага C2d домена у модуля не было вовсе: C2c перенёс владение данными, а
правила учёта оставались в `billing/services/`. Разделение на два файла
появилось вместе с правилами.
"""
